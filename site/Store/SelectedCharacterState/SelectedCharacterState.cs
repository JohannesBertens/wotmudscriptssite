namespace site.Store.SelectedCharacterState
{
    public class SelectedCharacterState
    {
        public string SelectedCharacter { get; }

        public SelectedCharacterState(string selectedCharacter)
        {
            SelectedCharacter = selectedCharacter;
        }
    }
}
